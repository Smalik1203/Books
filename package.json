{
  "name": "learnlab-books",
  "version": "1.0.0",
  "private": true,
  "description": "Print-ready textbooks. Fixed-page HTML to PDF, A4 and B5 editions, no runtime.",
  "type": "commonjs",
  "scripts": {
    "studio": "node build/serve.mjs",
    "build": "node build/build.mjs class-9 --pdf --png",
    "press": "node build/build.mjs class-9 --pdf --bleed"
  },
  "dependencies": {
    "katex": "^0.16.11",
    "sharp": "^0.35.4"
  }
}
