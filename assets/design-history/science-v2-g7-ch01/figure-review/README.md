# Chapter 1 illustration and caption review

User request: remove the unnecessary additional sentences styled as image titles
and stop repeating the folded-plane fallback across the chapter.

The original pages are preserved in `before.json`. The redundant candle-to-sunlight
bridge was removed; its teacher-led fire-safety instruction remains as body text.
The eclipse mechanism remains in regular, justified prose, with its scientific
meaning retained. The short labels above it identify the two separate views.

Four distinct transparent PNG illustrations replace the fallback plane on pages
8–11: contexts for “make it half”, observing and recording a leaf, measurement
tools, and rainwater on paving beside soil. They match those pages' questions,
evidence, summary, and assessment respectively. Accessibility descriptions remain
in SVG titles; no generic printed captions accompany these four illustrations.
All assets are placed at 720 × 280 units with preserved proportions, and no
existing instructional image is reduced. The plane images remain in Activity 1.1,
where comparing the two folds is the actual task.

`generated-art.json` records the built-in image-generation prompts and source
files. The final PNGs live in `figures/class-7/science/ch01-v2/`.

Validation: 11 pages, image on each page, distinct closing assets, text/figure
bounds, original scientific content and safety checks, justified prose checks,
and visual review of all final reading-PDF pages. Other chapters are unchanged.
