# Diary illustration

The user requested a PNG with no background, integrated with Activity 3.1.
The original `figures/reference/food/p002-diary.png` remains unchanged.
The selected built-in imagegen output is saved as
`figures/reference/food/p002-diary-transparent.png` and placed beside the
numbered instructions, directly on the activity panel's background.

Prompt used with the original image as the edit target:

> Use case: background-extraction. Edit target: the supplied blue ink Food Diary spiral notebook and pencil illustration. Remove only the exterior white rectangular background and pale blue exterior blob behind the objects. Output a PNG with true alpha transparency outside the notebook, spiral binding and pencil, including transparent holes between spiral loops. Keep opaque white notebook paper, the original blue line art, exact existing notebook table text, pencil, yellow accent rays, proportions and details unchanged. Clean antialiased edges, no checkerboard drawn into pixels, no new background or shadow. This asset will sit directly on a pale teal textbook activity panel.
